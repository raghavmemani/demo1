apiVersion: batch/v1
kind: Job
metadata:
  name: hello-test
spec:
  template:
    spec:
      containers:
        - name: test
          image: curlimages/curl
          command:
            - sh
            - -c
            - |
              curl http://hello-service:8080/api/hello
      restartPolicy: Never
