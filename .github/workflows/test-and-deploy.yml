name: Test and Deploy

on:
  workflow_dispatch:
    inputs:
      artifact_name:
        description: "Enter artifact name (see artifact-list.txt)"
        required: true
        type: choice
        options:
          # AUTO-GENERATED-OPTIONS-START
          - placeholder
          # AUTO-GENERATED-OPTIONS-END

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Print selected artifact
        run: |
          echo "Selected artifact: ${{ github.event.inputs.artifact_name }}"
